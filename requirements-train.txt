# Training & data-pipeline dependencies — needed for the OFFLINE jobs only
# (train_forecast_models, fit_jump_diffusion_params, precompute_serving_cache,
# calibration, data refresh). The serving containers do NOT install these.
#
# Install for offline work:  pip install -r requirements.txt -r requirements-train.txt

-r requirements.txt

xgboost>=2.0.0
scikit-learn>=1.3.0
yfinance>=0.2.40
beautifulsoup4>=4.12.0
requests>=2.31.0
curl_cffi>=0.6.0
matplotlib>=3.8.0
# shap is optional (explainability notebook) and conflicts with numpy>=2.5 via numba;
# install in a separate env if needed. Left out here on purpose.
